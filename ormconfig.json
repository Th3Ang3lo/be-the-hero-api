{
  "name": "default",
  "type": "sqlite",
  "database": "db.sqlite",
  "synchronize": true,
  "entities": ["./src/models/**/*.ts"],
  "migrations": ["./src/database/migrations/*.ts"],
  "cli": {
    "migrationsDir": "./src/database/migrations/"
  }
}
